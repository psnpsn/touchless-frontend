export interface Tapwater {
    id: string;
    ipA: string;
    gatewayId: string;
    sensorIds: string[];
    status: string;
    boardModel: string;
    tapModel: string;
    installDate: string;
    currentVersion: string;
}
